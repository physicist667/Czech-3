services:
  - type: web
    name: czech-learning-app
    runtime: docker
    plan: free           # free | starter | standard
    region: frankfurt     # frankfurt | amsterdam | oregon | singapore
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
